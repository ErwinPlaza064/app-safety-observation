{
    "$schema": "https://railway.app/railway.schema.json",
    "_comment": "Deploy trigger - 2026-01-13 07:41",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "php artisan migrate --force && php artisan db:seed --class=SuperAdminSeeder --force && php artisan db:seed --class=AdminUserSeeder --force && php artisan db:seed --class=AreaSeeder --force && php artisan serve --host=0.0.0.0 --port=$PORT",
        "healthcheckPath": "/health",
        "healthcheckTimeout": 300
    }
}
